# sabre_project

BioSTEAM model for Sargassum biorefinery (SABRE). Current scope: AD → membrane biogas upgrading with YAML-driven assumptions + first-pass TEA hooks.

## Setup
```bash
conda create -n sabre python=3.10 -y
conda activate sabre
pip install -e